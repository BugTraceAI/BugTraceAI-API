mcp[cli]>=1.0.0,<2
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
schemathesis>=3.20.0
offat>=0.13.0
arjun>=2.2.1
python-dotenv>=1.0.0
httpx>=0.25.0
PyYAML>=6.0.0
# Keep the container's built-in regression suite runnable; pytest is pulled by
# the scanner dependencies, while its async plugin is not.
pytest-asyncio>=0.23.0
